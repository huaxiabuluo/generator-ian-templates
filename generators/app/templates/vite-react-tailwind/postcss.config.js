/** @type {import('postcss-load-config').Config} */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {
      // browsers: ['> 1%', 'Chrome >= 89', 'Firefox ESR', 'Safari >= 14'],
    },
  },
};
